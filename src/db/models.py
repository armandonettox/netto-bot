# Estrutura das tabelas no Supabase:
#
# users: id, phone, name, monthly_income, created_at
# fixed_expenses: id, user_id, description, amount, due_day, category
# transactions: id, user_id, description, amount, category, date, created_at
